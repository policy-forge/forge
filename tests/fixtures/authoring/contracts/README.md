# Synthetic authoring contract fixtures

These fixtures contain only original synthetic identifiers and drafting metadata.
They test closed parsing and forward-version rejection. Pins in contract-only
fixtures are placeholders; executable CLI fixtures generate exact local hashes.
No fixture establishes human readiness, independent review, or release status.
